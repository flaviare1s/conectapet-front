# =============================
# Arquivo de exemplo de variáveis de ambiente (.env.example)
# =============================
#
# O QUE SÃO VARIÁVEIS DE AMBIENTE?
# Variáveis de ambiente são usadas para armazenar informações sensíveis ou de configuração
# (como chaves de API, senhas e IDs de serviços) fora do código-fonte. Isso mantém seu projeto
# mais seguro e facilita a configuração em diferentes ambientes (desenvolvimento, produção, etc).
#
# COMO USAR ESTE ARQUIVO:
# 1. Copie este arquivo e renomeie para ".env" na raiz do projeto.
# 2. Preencha os valores reais das variáveis abaixo.
# 3. NÃO suba o arquivo .env para o Git (ele já está no .gitignore).
# 4. Compartilhe apenas o .env.example para que outros saibam quais variáveis precisam ser definidas.
#
# COMO ACESSAR NO CÓDIGO:
# No frontend (Vite/React), use: import.meta.env.NOME_DA_VARIAVEL
# Exemplo: import.meta.env.VITE_EMAILJS_SERVICE_ID
#
# ATENÇÃO:
# Nunca coloque dados realmente sensíveis no .env do frontend, pois tudo pode ser lido pelo usuário final.
#
# =============================
# Exemplo de variáveis para EmailJS
VITE_EMAILJS_SERVICE_ID=seu_service_id
VITE_EMAILJS_TEMPLATE_ID=seu_template_id
VITE_EMAILJS_PUBLIC_KEY=sua_public_key
# =============================
